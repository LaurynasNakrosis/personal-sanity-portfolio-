const adminConfig = {
  projectId: "w186ajj8",
  dataset: "production",
  apiVersion: "2024-04-03",
  useCdn: false,
  token: process.env.SANITY_API_TOKEN, // This needs to be set in your environment variables
};

export default adminConfig; 